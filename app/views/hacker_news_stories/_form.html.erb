<%= form_for(hacker_news_story) do |f| %>
  <% if hacker_news_story.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(hacker_news_story.errors.count, "error") %> prohibited this hacker_news_story from being saved:</h2>

      <ul>
      <% hacker_news_story.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :by %>
    <%= f.text_field :by %>
  </div>

  <div class="field">
    <%= f.label :story_id %>
    <%= f.number_field :story_id %>
  </div>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :url %>
    <%= f.text_field :url %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
